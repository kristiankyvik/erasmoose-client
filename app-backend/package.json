{
  "name": "api.erasmoose.com",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "micro",
    "dev": "micro-dev -p 4000",
    "deploy": "now deploy -e ENV=production"
  },
  "dependencies": {
    "dotenv": "^4.0.0",
    "graphql": "@latest",
    "graphql-server-micro": "@latest",
    "graphql-tools": "@latest",
    "micro": "@latest",
    "micro-cors": "@latest",
    "request": "^2.83.0",
    "request-promise": "^4.2.2"
  },
  "now": {
    "name": "api.erasmoose.com",
    "alias": "www.api.erasmoose.com"
  },
  "license": "MIT",
  "devDependencies": {
    "micro-dev": "^1.2.3"
  }
}
